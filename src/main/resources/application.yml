#jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
#${DB_USERNAME}
#${DB_PASSWORD}

spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/moto
        username: postgres
        password: 123
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: create-drop
        generate-ddl: true